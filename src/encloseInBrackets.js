function solution(inputString) {
    return `(${inputString})`;
}

module.exports = solution;

// npm init -y
// npm i jest-cli -g
// package.json: "scripts": { "test": "jest" }
// npm test encloseInBrackets

// alternative solution
